<?xml version="1.0" encoding="UTF-8" ?>
<properties>

    <query key="comment.target">
        INSERT INTO comments (board_id, creator_id, content)
        VALUES (?, ?, ?)
    </query>

    <query key="comment.adminselect">
        SELECT * FROM comments
        WHERE board_id = ?
    </query>

    <query key="comment.userselect">
        SELECT * FROM comments
        WHERE board_id = ? AND creator_id = ?
    </query>

    <query key="comment.update">
        UPDATE comments
        SET content = ?
        WHERE comment_id = ?
    </query>

    <query key="comment.delete">
        DELETE FROM comments
        WHERE comment_id = ?
    </query>

</properties>