<?xml version="1.0" encoding="UTF-8" ?>
<properties>

    <query key="city.findAll">
        SELECT * FROM City;
    </query>

    <query key="city.findById">
        SELECT * FROM City WHERE city_id = ?
    </query>

    <query key = "city.insertCity">
        insert INTO City
            (city_name, description, creator_id, status)
        VALUES
            (?, ?, ?, ?)
    </query>

    <query key="city.updateCity">
        UPDATE City
        set city_name = ?,
            description = ?
        WHERE city_id = ?
    </query>

    <query key="city.updateStatus">
        UPDATE City
        set status = ?
        WHERE city_id = ?
    </query>

</properties>