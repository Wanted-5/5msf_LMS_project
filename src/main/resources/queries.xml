<?xml version="1.0" encoding="UTF-8" ?>
<properties>
    <query key="comment.target">
    INSERT INTO Comments (board_id, creator_id, content) VALUES (?, ?, ?)
    </query>
    <query key ="comment.adminselect">
        SELECT * FROM comments
        WHERE board_id = ?
    </query>
    <query key ="comment.userselect">
        SELECT * FROM comments
        WHERE board_id = ?, user_id = ?
    </query>
    <query key="users.findByUsername">
        SELECT * FROM Users WHERE username = ?
    </query>
    <query key="users.insertUser">
        insert into Users
        (username, password, email, name, nickname, phone_number, address, gender, introduction, role)
        VALUES
        (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    </query>

</properties>