<?xml version="1.0" encoding="UTF-8" ?>
<properties>

    <query key="quizsub.submit">
        INSERT INTO quiz_submissions (quiz_id, user_id, submitted_answer, is_correct)
        VALUES (?, ?, ?, ?)
    </query>

    <query key="quizsub.alreadySubmission">
        SELECT COUNT(*) FROM quiz_submissions
        WHERE quiz_id = ?
        AND user_id = ?
    </query>

</properties>