<?xml version="1.0" encoding="utf-8" ?>
<properties>

    <query key="village.insertVillage">
        INSERT INTO Village
        (city_id, village_name, description,
        invite_code, start_date, end_date)
        VALUES
        (?, ?, ? ,?, ?, ?)
    </query>

    <query key="village.findByInviteCode">
        SELECT * from Village
        WHERE invite_code = ?
    </query>

</properties>